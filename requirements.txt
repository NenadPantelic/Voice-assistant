SpeechRecognition
NOTE: for Windows, if you're using python 3.7, you need to install precompiled binaries for compatibility sake, so install
pipwin first)
pyaudio
pydub
ffmpeg (for Windows)
pocketsphinx
google-cloud-speech (only for google cloud)
pyowm
wikipedia
omdb
googletrans
gmail
pip3 install git+https://github.com/alberanid/imdbpy
googlesearch
pyserial
pyttsx3 (requires sudo apt-get update && sudo apt-get install espeak)
gTTS